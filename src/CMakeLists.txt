file(GLOB_RECURSE _sources ${CMAKE_CURRENT_LIST_DIR}/*.cpp  CONFIGURE_DEPEND)
file(GLOB_RECURSE _cuda_sources ${CMAKE_CURRENT_LIST_DIR}/*.cu  CONFIGURE_DEPEND)
target_sources(splat PRIVATE ${_sources} ${_cuda_sources})
unset(_sources)
unset(_cuda_sources)